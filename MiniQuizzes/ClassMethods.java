package MiniQuizzes;

public class ClassMethods {
    public static void main(String[] args) {
        //your_method(4, 5) -----> returns true
        //your_method(3, 3) -----> returns false
    }

    

    /*
     * Create a public (static) method that:
     * 1. takes two parameters
     * 2. calculates first param raised to power of second param
     * 3. returns true if that number is greater than 100, false otherwise
     * HINT: use Math.pow(a, b) -> raises a to b power
     */

    // YOUR CODE HERE
}
