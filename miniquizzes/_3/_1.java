package miniquizzes._3;

public class _1 {
    public static void main(String[] args) {

        // Use this class definition for the questions below:

        // public class Pet {
        //     private String name;
        //     private int age;
        //
        //     public Pet(String n, int a) {
        //         name = n;
        //         age = a;
        //     }
        //
        //     public String getName() { return name; }     // method A
        //     public void setAge(int a) { age = a; }       // method B
        //     public void birthday() { age++; }            // method C
        //     public int getAge() { return age; }          // method D
        //
        //     public String toString() {
        //         return name + " (age " + age + ")";
        //     }
        // }

        // PART 1: Identify each method:
        // Method A is a: (constructor / accessor / mutator)
        // Method B is a: (constructor / accessor / mutator)
        // Method C is a: (constructor / accessor / mutator)
        // Method D is a: (constructor / accessor / mutator)

        // PART 2: Default values
        // If a class has these instance variables and NO constructor, what are their default values?
        // int count     ->
        // double price  ->
        // boolean found ->
        // String name   ->

        // PART 3: What does this code print?
        // Pet p1 = new Pet("Max", 3);
        // p1.birthday();
        // p1.birthday();
        // System.out.println(p1);

        // PART 4: Will this code compile? Why or why not?
        // Pet p = new Pet("Luna", 2);
        // System.out.println(p.name);

    }
}
